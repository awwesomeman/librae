- 使用者更新偏好：後續 LLM 協作改用 Claude CLI 或 Gemini CLI，不走 ACP；策略開發偏 Claude CLI，研究/快速對照可用 Gemini CLI。
- 與使用者完成 Streamlit 儀表板多輪迭代：主標與區塊命名、卡片層級、表格固定高度與 scroll、圖表 legend 位置、Performance/Parameter 結構反覆收斂到可驗收版本。
- 使用者明確偏好：後續協作優先用 Claude CLI / Gemini CLI，不走 ACP；已記錄於 USER.md 與當日記憶。
- 針對 UI schema 命名規範，已確認團隊共識趨向「全部 snake_case（大分類/子分類/實際 key）」以降低前後端映射成本與維護風險。
- 使用者要求 Parameter 頁籤資料治理原則：Value 欄僅放值、Description 放單位與語義；子鍵可展開；策略邏輯不放 Strategy 參數表；避免 key 寫死單位（例如 *_pct / *_ticks）。
- 使用者要求 Performance Analysis 與 Metric Guide 順序必須同源同步（single source of truth），並反覆驗收排序與比較高亮邏輯。
- 已建立/調整團隊角色設定：PM(main)、後端工程師(quant-dev)、量化策略研究員(quant-monitor)、前端工程師(frontend-dev)；但目前會話 allowlist 尚未放行 frontend-dev，需新會話或後端放行才可直接調度。
- 已啟動兩條並行任務：
  - quant-dev：交易框架/資料流藍圖（NautilusTrader 預設）與參數 schema 清理實作。
  - quant-monitor：框架選型與命名治理審核。
- quant-dev 已產出 `docs/trading_framework_blueprint.md`（commit: 39d741c），聚焦 NautilusTrader + Binance/Shioaji legacy 漸進整合（Strangler pattern、canonical contract、backtest/live parity）。
- 使用者確認團隊運作方式：後續持續動態調整角色職能與工作章程。
- 使用者新增 schema 規範：若需單位說明，統一放在 Description，格式固定為 `(unit: xxx)`，不放在 key 或 value。
- 使用者要求 schema 規範再新增並立即落地：凡需單位說明，統一僅在 Description 以 `(unit: xxx)` 呈現；不得出現在 key/value。
- 使用者要求先行改版：已重新派發 quant-dev 新 run，要求全面套用 unit 規範並再次清理 snake_case 一致性。
