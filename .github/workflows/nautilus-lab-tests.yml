name: nautilus-lab-tests

on:
  push:
    paths:
      - "nautilus_lab/**"
      - ".github/workflows/nautilus-lab-tests.yml"
  pull_request:
    paths:
      - "nautilus_lab/**"
      - ".github/workflows/nautilus-lab-tests.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: nautilus_lab
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .[test]

      - name: Run contract + streamlit smoke tests
        run: |
          pytest -q tests/test_contracts.py tests/test_streamlit_schema_smoke.py
