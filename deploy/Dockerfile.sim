FROM python:3.12-slim

WORKDIR /app
ENV PYTHONUNBUFFERED=1

# Install dependencies
COPY pyproject.toml .
RUN pip install --no-cache-dir . ccxt

# Copy source (filtered by .dockerignore)
COPY . .

# No CMD — sim_start.sh passes the command via docker run
